import Vue from 'Vue';
import Vuex from 'Vuex';

const state = {
  listProxy = []
};

const mutation = {};

const action = {

};

const getters = {

}

Vue.use(Vuex);

const store = new Vuex.Store({
	state,
	mutations,
	actions,
	getters
});

export default store;
 